# vsys-node-api
API for volunteer management system.

TODO LIST
- opportunities API sql fixes, using the right tables, reducing computing resources used.
- admin dashboard with cruds for all entities in the system
- unify styles accross the app(tailwind, materialUI or bootstrap)
- populate the DB with dummy data for testing purposes
- include centers as third optional user type for posting opps and accepting/rejecting volunteering requests, reducing admin importance to just oversee and approve opps instead of doing the whole crud tasks  
- 